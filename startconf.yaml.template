appID: "${APP_ID}"
etcd:
  endpoints: "${ETCD_NODESLIST}"
  auth:
    username: "${ETCD_LOGIN}"
    password: "${ETCD_PASSWORD}"
  tls:
    ca_file: certs/ca.crt
    cert_file: certs/client.crt
    key_file: certs/client.key
